@page "/Help"
@inject IJSRuntime JS
@using Dates.Components.Layout

<Sidebar />

<link href="css/Help.css" rel="stylesheet" />

<div class="help-container">
    <h2 class="username-title">👋 مرحباً، @username</h2>
    <h3 class="main-title">📘 إرشادات استخدام النظام</h3>

    <ol class="guidelines">
        <li><strong>🧾</strong> يمكنك تسجيل الفواتير بسهولة من خلال صفحة "إضافة فاتورة" مع إدخال جميع التفاصيل بدقة.</li>
        <li><strong>📅</strong> تأكد من إدخال التاريخ الصحيح لكل عملية شراء حتى تظهر في التقارير والتنبيهات بشكل سليم.</li>
        <li><strong>💰</strong> حالة الدفع "غير مدفوع" تظهر تلقائيًا إذا لم يتم تحديد خيار الدفع، ويمكن تعديلها لاحقًا.</li>
        <li><strong>🧮</strong> صفحة "كل الفواتير" تتيح لك مراجعة جميع فواتيرك، ويمكنك تصفيتها حسب التاريخ أو نوع المنتج.</li>
        <li><strong>📊</strong> الإحصائيات الأسبوعية تساعدك في متابعة الأداء والمبيعات بشكل مختصر ومرتب.</li>
        <li><strong>📱💻</strong> التصميم متجاوب بالكامل ويعمل على الهاتف والكمبيوتر لتتمكن من الوصول للفواتير في أي وقت.</li>
        <li><strong>👥</strong> يمكنك إنشاء أكثر من حساب باسم مستخدم وكلمة مرور مختلفين، مثلًا لحساب تمر وحساب تفاح، بدون استخدام بريد إلكتروني.</li>
        <li><strong>🔐</strong> تأكد من حفظ اسم المستخدم وكلمة المرور الخاصة بكل حساب للوصول إليه لاحقًا بسهولة.</li>
        <li><strong>🚨</strong> التنبيهات تظهر لك الفواتير غير المدفوعة، وإذا مر أكثر من 5 أيام عليها ستظهر بإشارة حمراء.</li>
        <li><strong>📝</strong> لا تنس كتابة ملاحظاتك داخل كل فاتورة لمراجعتها مستقبلًا، مثل اسم المورد أو تفاصيل إضافية.</li>
    </ol>
</div>

@code {
    private string? username;

    protected override async Task OnInitializedAsync()
    {
        username = await JS.InvokeAsync<string>("sessionStorage.getItem", "username");
    }
}
